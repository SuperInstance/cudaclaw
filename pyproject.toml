[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cudaclaw"
version = "1.0.0"
description = "Cocapn Fleet module — cudaclaw"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
keywords = ["cocapn", "fleet"]

[project.scripts]
cudaclaw = "agent:main"
